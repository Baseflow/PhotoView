buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
  }
}

allprojects {
  group = 'uk.co.senab.photoview'
  version = '1.2.2-SHAPSHOT'

  repositories {
    mavenCentral()
  }

  tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
  }
}

